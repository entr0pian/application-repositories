apiVersion: platform.taskapp.io/v1alpha1
kind: Release
metadata:
  name: checkout-dev
  labels:
    platform.taskapp.io/component: checkout
spec:
  componentRef:
    name: checkout
  environment: dev
  version: "latest"
  bindings:
    database:
      enabled: true
      ref: checkout-db
